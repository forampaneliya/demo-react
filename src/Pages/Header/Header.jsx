import "./Header.css"

function Header()
{
    function searchData(e)
    {
      e.preventDefault()
      let newdata=e.target.search.value;
      setser(newdata)
    }

    return(
        <>
       
        
        </>
    )
}
export default Header;